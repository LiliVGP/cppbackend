#include "request_handler.h"

namespace http_handler {

	// В этом файле сейчас нет необходимости в реализации, так как все шаблонные методы
	// определены в header. Он остается для возможных будущих нешаблонных реализаций.

}  // namespace http_handler